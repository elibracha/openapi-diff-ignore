version: 1.0.0
info: this is a sample ignore file to silence changes in openapi-diff
project: sample-service
paths:
  /*:
    post:
      security:
        - petstore_auth:
            - 'write:pets'
            - 'read:pets'

  /store:
    ignore-type: all
